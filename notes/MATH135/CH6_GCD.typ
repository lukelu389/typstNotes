#import "../../preamble.typ": *

#let Title = "CH 6- Greatest Common Divisor"
#set-title(Title)
#meta(title: Title, author: "Luke Lu")

== Theorem BBD
#info[*Bound By Divisibility* \
$forall a, b in ZZ, "if" b divides a "and" a!= 0, "then" b <= |a|$]

\
== Division Algorithm 
\
$"For all" a in ZZ, b "in positive integers, there exist unique integers" q "and" r "s.t."\
 a = q b+r, quad quad quad 0<=r<b$

 